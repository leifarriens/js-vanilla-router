{
  "name": "vanilla-router-js",
  "version": "0.3.0",
  "description": "Simple Javascript Router",
  "main": "Router.js",
  "scripts": {
    "start": "npm run build",
    "build": "parcel build Router.js --no-source-maps --no-cache --global Router --out-file Router.min.js --out-dir ./"
  },
  "keywords": [
    "router",
    "vanillarouter"
  ],
  "author": "Leif Arriens",
  "license": "MIT",
  "dependencies": {
    "animate.css": "^3.7.0"
  }
}
